<ul>
  <% comments.each do |comment| %>
    <% if comment.id.present? %>

        <p><%= comment.content %></p>
           <%= link_to '削除',blog_comment_path (comment.id), method: :delete, remote:true %>
    <% end %>
  <% end %>
</ul>
